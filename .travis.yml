language: java

jdk: openjdk8

addons:
  sonarcloud:
    organization: "eyedema-palai103-school-agenda"
    token: ${SONAR_TOKEN}

# skip installation step
install: true

script:
  - mvn clean verify -Pjacoco coveralls:report sonar:sonar org.pitest:pitest-maven:mutationCoverage
